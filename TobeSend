Get-VBRJob | Select Name, @{N="HasSchedule";E={
    $s = $_.GetScheduleOptions()
    
    if ($s.OptionsDaily.Enabled -or
        $s.OptionsMonthly.Enabled -or
        $s.OptionsPeriodically.Enabled -or
        $s.OptionsContinuous.Enabled) {
        "Yes"
    } else {
        "No"
    }
}}
